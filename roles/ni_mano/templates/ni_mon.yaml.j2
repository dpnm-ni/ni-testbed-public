kafka_client:
  consumer:
    bootstrap.servers: {{ KAFKA_SERVER_ADDR }}:9092
    enable.auto.commit: False
    enable.auto.offset.store: False
    # debug: 'cgrp,fetch,topic,broker,protocol'

openstack_client:
  auth:
    auth_url: "http://{{ groups['controller'][0] }}/identity/v3"
    username: "admin"
    password: "{{ ADMIN_PASSWORD }}"
    project_name: "demo"
    user_domain_id: "default"
    project_domain_id: "default"

influxdb_client:
  server: "{{ INFLUXDB_SERVER_ADDR }}"
  database: ni
